# This file shows how to configure secrets for local Streamlit development
# Place this file at .streamlit/secrets.toml (create the .streamlit directory if it doesn't exist)
# For production deployment on Streamlit Cloud, add these secrets in the app settings

OPENAI_API_KEY = "your-openai-api-key-here"

# Optional: Other configuration settings
DEFAULT_MODEL = "gpt-4"
DEBUG = true
